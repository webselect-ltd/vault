var CredentialSummary = (function () {
    function CredentialSummary(credentialid, masterkey, userid, description, username, password, url, weak) {
        this.credentialid = credentialid;
        this.masterkey = masterkey;
        this.userid = userid;
        this.description = description;
        this.username = username;
        this.password = password;
        this.url = url;
        this.weak = weak;
    }
    return CredentialSummary;
}());
//# sourceMappingURL=CredentialSummary.js.map